<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 1) Check overdue loans daily -->
    <record id="cron_check_overdue_loans" model="ir.cron">
        <field name="name">Check overdue loans</field>
        <field name="model_id" ref="model_library_loan"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_overdue()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="priority">10</field>
        <field name="active">True</field>
    </record>

    <!-- 2) Send return reminders 2 days before due date -->
    <record id="cron_send_return_reminders" model="ir.cron">
        <field name="name">Send return reminders</field>
        <field name="model_id" ref="model_library_loan"/>
        <field name="state">code</field>
        <field name="code">model._cron_send_return_reminders()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="priority">10</field>
        <field name="active">True</field>
    </record>

    <!-- 3) Expire reservations after 2 days -->
    <record id="cron_expire_reservations" model="ir.cron">
        <field name="name">Expire library reservations</field>
        <field name="model_id" ref="model_library_reservation"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_reservations()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="priority">10</field>
        <field name="active">True</field>
    </record>
</odoo>
